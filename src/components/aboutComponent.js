import React, { Component } from 'react';

export default (props) => {
    return (
        <div>
            <p>
                Github Analyzer will give aggregated statistics about your account.<br />
                <b>Note: We don't store any passwords or user data.</b>
            </p>
        </div>
    );
}